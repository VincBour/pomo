import { styled } from "@mui/material";

export const DivMarginThree = styled("div")(({ theme }) => ({
  margin: theme.spacing(3),
}));

export const DivMarginTwo = styled("div")(({ theme }) => ({
  margin: theme.spacing(2),
}));
